{
  "name": "ai-native-document-editor",
  "version": "1.0.0",
  "description": "Full-stack AI document editor built for Render deployment",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js",
    "dev": "npm start"
  },
  "dependencies": {}
}
